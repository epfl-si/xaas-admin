events {
}
http {
       server {
                listen         8080;
                server_name    localhost;
                charset        utf-8;

                location / {
                        proxy_pass http://xaas-django:80/;
                        proxy_set_header Host $host:$proxy_port;
                }

        }
}

