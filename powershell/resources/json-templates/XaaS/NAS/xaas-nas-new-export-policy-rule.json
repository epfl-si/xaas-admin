{
    "clients": [
      {
        "match": "{{clientMatch}}"
      }
    ],
    "protocols": [
      "{{protocol}}"
    ],
    "ro_rule": [
      "{{roRule}}"
    ],
    "rw_rule": [
      "{{rwRule}}"
    ],
    "superuser": [
      "{{superUser}}"
    ]
  }