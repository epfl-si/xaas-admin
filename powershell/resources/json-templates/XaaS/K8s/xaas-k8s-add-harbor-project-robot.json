{
    "access": [
      {
        "action": "push",
        "resource": "/project/{{projectId}}/repository"
      },
      {
        "action": "pull",
        "resource": "/project/{{projectId}}/repository"
      }
    ],
    "name": "{{name}}",
    "expire_at": "{{expireAt}}",
    "description": "{{description}}"
  }