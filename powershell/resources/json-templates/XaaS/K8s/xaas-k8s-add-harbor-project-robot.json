{
  "name": "{{name}}",
  "duration": "{{durationDays}}", // doit être un INT, pas un STRING
  "description": "{{description}}",
  "disable": false,
  "level": "project",
  "permissions": [
    {
      "namespace": "{{projectName}}",
      "kind": "project",
      "access": [
        {
          "resource": "repository",
          "action": "{{robotType}}"
        }
      ]
    }
  ]
}