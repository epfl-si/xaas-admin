{
    "access": [
      {
        "action": "{{robotType}}",
        "resource": "/project/{{projectId}}/repository"
      }
    ],
    "name": "{{name}}",
    "expire_at": "{{expireAt}}",
    "description": "{{description}}"
  }