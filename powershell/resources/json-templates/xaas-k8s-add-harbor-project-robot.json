{
    "access": [
      {
        "action": "push",
        "resource": "/project/{{projectId}}/repository"
      }
    ],
    "name": "{{name}}",
    "expire_at": "{{expireAt}}",
    "description": "{{description}}"
  }