{
    "name": "{{name}}",
    "typeId": "com.github",
    "projectId": "{{projectId}}",
    "config": {
                "path": "{{path}}",
                "branch": "{{branch}}",
                "repository": "{{repository}}",
                "contentType": "{{contentType}}",
                "projectName": "{{projectName}}",
                "integrationId": "{{integrationId}}"
            }
}