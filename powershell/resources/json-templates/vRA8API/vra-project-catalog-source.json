{
    "name": "{{name}}",
    "typeId": "com.vmw.blueprint",
    "config": {
        "sourceProjectId": "{{catalogProjectId}}"
    },
    "projectId": "{{catalogProjectId}}",
    "global": true
}