{
    "name": "{{name}}",
    "typeId": "com.github",
    "projectId": "{{projectId}}",
    "syncEnabled": true,
    "config": {
                "path": "{{path}}",
                "branch": "{{branch}}",
                "repository": "{{repository}}",
                "contentType": "{{contentType}}",
                "projectName": "{{projectName}}",
                "integrationId": "{{integrationId}}"
            }
}