{
    "aggregates": [
      {
        "name": "{{aggregateName}}",
        "uuid": "{{aggregateUUID}}"
      }
    ],
    "svm": {
      "name": "{{svmName}}",
      "uuid": "{{svmUUID}}"
    },
    "name": "{{volName}}",
    "size": {{sizeInBytes}}
}