{
    "aggregates": [
      {
        "name": "{{aggregateName}}",
        "uuid": "{{aggregateUUID}}"
      }
    ],
    "svm": {
      "name": "{{svmName}}",
      "uuid": "{{svmUUID}}"
    },
    "nas": {
      "security_style":"{{securityStyle}}",
      "path": "{{mountPath}}"
    },
    "name": "{{volName}}",
    "size": "{{sizeInBytes}}"
}