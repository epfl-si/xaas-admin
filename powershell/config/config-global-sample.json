/* == Configuration globale à tous les environnement == */
{
    "mail": {
        "admin": "", // adresse mail de l'admin
        "capacityAlert": "" // Adresse mail par défaut à laquelle envoyer les mails de "capacity alert"
    }
}